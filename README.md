# 📚 QuizCard - Система изучения языков с карточками

**Современная MVC-архитектура | PHP 8+ | Безопасность | Многоязычность**

Веб-приложение для преподавателей и учеников с карточками для изучения иностранных слов и системой интервальных повторений.

## 🏗️ Архитектура проекта

QuizCard построен на современной **MVC (Model-View-Controller) архитектуре** для обеспечения масштабируемости, поддерживаемости и производительности.

### 📁 Структура проекта:

```
QuizCard/
├── app/                    # MVC ядро приложения
│   ├── Config/            # Конфигурационные файлы
│   ├── Controllers/       # Бизнес-логика (37 контроллеров)
│   ├── Models/           # Модели данных (15 классов)
│   └── Views/            # Представления и шаблоны
├── public/               # Публичная папка (входная точка)
│   ├── index.php         # Front Controller
│   ├── css/              # Стили
│   ├── js/               # JavaScript
│   └── uploads/          # Пользовательские файлы
├── database/             # Миграции и схема БД
├── docs/                 # Документация
├── vendor/               # Composer зависимости
└── old/                  # Архив предыдущих версий
```

### 🎯 Преимущества MVC архитектуры:

- **Разделение ответственности** - четкое разделение логики, данных и представления
- **Масштабируемость** - легко добавлять новый функционал
- **Поддерживаемость** - простота сопровождения и отладки
- **Тестируемость** - изолированные компоненты для unit-тестов
- **SEO-friendly URLs** - человекопонятные адреса без .php

## ✨ Основные возможности

### Для- Прохождение назначенных тестов

- Ограничение времени (если установлено)
- Мгновенные результаты
- История попыток

## 🛡️ Система безопасности

QuizCard включает комплексную систему безопасности, соответствующую современным стандартам:

### Основные компоненты безопасности:

- 🔐 **Защита от CSRF атак** - токены для всех форм
- 🛡️ **Валидация и санитизация данных** - проверка всех пользовательских вводов
- 🚫 **Rate Limiting** - защита от брут-форса и DDoS атак
- 📝 **Логирование событий безопасности** - детальный мониторинг
- 🔒 **Улучшенное хеширование паролей** - BCRYPT с автоматическим перехешированием
- 🌐 **Защита на уровне веб-сервера** - настроенные .htaccess правила
- 🔑 **Переменные окружения** - безопасное хранение секретных данных

### Панель безопасности для преподавателей:

- 📊 **Статистика безопасности** - количество атак, попыток входа
- 📋 **Журнал событий** - детальные логи с фильтрацией
- ⚠️ **Предупреждения** - уведомления о подозрительной активности
- 🎯 **Фильтрация по времени** - анализ событий за разные периоды
- 👥 **Персонализация** - каждый преподаватель видит только свои события и события своих студентов

### Автоматическая защита:

- **CSRF Protection** - автоматическая генерация и проверка токенов
- **Input Validation** - проверка всех данных форм
- **SQL Injection Prevention** - подготовленные запросы
- **XSS Protection** - экранирование выводимых данных
- **Session Security** - безопасное управление сессиями
- **File Upload Security** - проверка типов и размеров файлов

### Мониторинг и логирование:

- Все события безопасности записываются в `logs/security.log`
- Автоматическая ротация логов при превышении размера
- Очистка старых записей для оптимизации производительности
- API для логирования пользовательских событий

📊 \*\*Статусы словдавателей:

- 👥 Управление учениками
- ✏️ Редактирование данных учеников (логин/пароль)
- 📝 Создание колод (тем) со словами
- 🎯 Назначение колод ученикам
- 📊 Просмотр статистики обучения
- 🖼️ Добавление изображений к словам
- 🎵 Добавление аудиофайлов к словам
- 📤 Массовый импорт слов из Excel и CSV
- 📋 Создание и управление тестами
- 🌍 Многоязычный интерфейс (русский, казахский, английский)
- ⚙️ Настройка дневных лимитов новых слов
- 📧 Система подтверждения email
- 📈 Детальная аналитика прогресса учеников
- 🛡️ Панель безопасности с мониторингом событий
- 🔐 Защита от CSRF атак и валидация данных

### Для учеников:

- 📚 Изучение назначенных колод
- 🧠 Система интервальных повторений
- 📈 Отслеживание прогресса
- 🎯 Дневные лимиты новых слов
- 📝 Прохождение тестов
- 🌍 Переключение языка интерфейса
- 📊 Подробная статистика обучения
- 🔒 Безопасная аутентификация и защита данных

## 🚀 Установка

### 1. Загрузка файлов

Загрузите все файлы проекта на ваш веб-хостинг.

### 2. Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:

   ```bash
   cp .env.example .env
   ```

2. Отредактируйте файл `.env` с вашими настройками:

   ```env
   # База данных
   DB_HOST=localhost
   DB_NAME=quizcard
   DB_USER=your_username
   DB_PASS=your_password

   # Email настройки
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-app-password

   # Безопасность
   CSRF_SECRET=your-random-secret-key-here
   RATE_LIMIT_REQUESTS=100
   RATE_LIMIT_WINDOW=3600
   ```

### 3. Настройка базы данных

1. Создайте базу данных MySQL
2. Настройки базы данных автоматически читаются из файла `.env`

### 4. Инициализация

**Для новых установок:**

1. Откройте `setup.php` в браузере
2. Следуйте инструкциям по настройке
3. Создайте первого преподавателя

**Для обновления существующих установок:**

1. Откройте `database/migrate.php` в браузере
2. Выполните автоматическую миграцию базы данных
3. Проверьте корректность обновления

## 📖 Использование

### Преподаватель:

1. Войдите в систему как преподаватель
2. Создайте колоды в разделе "Колоды"
3. Добавьте слова вручную или импортируйте из файла
4. Назначьте колоды ученикам
5. Отслеживайте прогресс учеников

### Ученик:

1. Войдите в систему как ученик
2. Изучайте назначенные колоды
3. Отвечайте на карточки
4. Отслеживайте свой прогресс

## 📤 Импорт слов

Поддерживается импорт из CSV и Excel файлов (.xlsx).

### Формат файла:

```csv
foreign_word,translation,image,audio
apple,яблоко,https://example.com/apple.jpg,https://example.com/apple.mp3
house,дом,,
car,машина,https://example.com/car.png,
```

### Колонки:

- **foreign_word** (или word) - иностранное слово
- **translation** (или translate) - перевод
- **image** (или images) - URL изображения (опционально)
- **audio** - URL аудиофайла (опционально)

## 🎵 Поддержка аудиофайлов

QuizCard поддерживает добавление аудиофайлов к словам для улучшения произношения:

### Функции:

- 🎧 **Воспроизведение** - встроенный HTML5 аудиоплеер
- 📤 **Загрузка** - поддержка MP3, WAV, OGG форматов
- 🔗 **Импорт** - добавление через URL или загрузка файлов
- 📱 **Адаптивность** - работает на всех устройствах

### Поддерживаемые форматы:

- **MP3** - основной формат (рекомендуется)
- **WAV** - высокое качество
- **OGG** - альтернативный формат

### Использование:

1. **Для преподавателей**: добавляйте аудио при создании/редактировании слов
2. **Для учеников**: прослушивайте произношение в карточках и тестах
3. **Импорт**: включайте столбец `audio` в CSV/Excel файлы

## 📧 Система подтверждения email

QuizCard включает систему подтверждения email для безопасности:

### Функции:

- ✉️ **Подтверждение email** - обязательная верификация для новых пользователей
- 🔗 **Токены верификации** - безопасные одноразовые ссылки
- ⏰ **Срок действия** - токены истекают через 24 часа
- 🔄 **Повторная отправка** - возможность запросить новое письмо
- 🧹 **Автоочистка** - удаление истекших токенов

### Настройка:

1. Настройте SMTP в `config/email_config.php`
2. Проверьте работу через `test_email.php`
3. При миграции выполните `database/migrate.php`

### Для пользователей:

- При регистрации придет письмо с ссылкой подтверждения
- Без подтверждения вход в систему ограничен
- Возможность запросить повторное письмо

## � Многоязычная поддержка

QuizCard поддерживает три языка интерфейса:

- 🇷🇺 **Русский** - язык по умолчанию
- 🇰🇿 **Казахский** - поддержка государственного языка Казахстана
- 🇬🇧 **Английский** - международный язык

### Переключение языка:

1. Используйте переключатель языков в верхней части страницы
2. Выбранный язык сохраняется в сессии
3. Все элементы интерфейса переводятся автоматически

### Для разработчиков:

- Переводы хранятся в `includes/lang/`
- Система автоматического fallback на английский язык
- Подробное руководство в `teacher/language_integration_guide.php`

## �🎯 Дневные лимиты

Преподаватели могут устанавливать дневные лимиты новых слов для колод:

- Ограничивает количество новых слов в день
- Помогает не перегружать учеников
- Настраивается индивидуально для каждой колоды

## � Система тестирования

QuizCard включает полноценную систему создания и прохождения тестов:

### Для преподавателей:

- Создание тестов с различными типами вопросов
- Настройка времени прохождения
- Автоматическая проверка ответов
- Просмотр результатов и статистики
- Предварительный просмотр тестов

### Для учеников:

- Прохождение назначенных тестов
- Ограничение времени (если установлено)
- Мгновенные результаты
- История попыток

## �📊 Статусы слов

- **Новое** - слово еще не изучалось (total_attempts = 0)
- **Изучается** - есть попытки, но менее 3 успешных повторений подряд
- **Изучено** - 3 или более успешных повторения подряд

## 🔧 Системные требования

- **PHP** 7.4+ с расширениями: PDO, ZIP, GD, cURL
- **MySQL** 5.7+ или MariaDB 10.2+
- **Веб-сервер** Apache/Nginx с поддержкой .htaccess
- **Память** минимум 128MB RAM для PHP
- **Дисковое пространство** минимум 100MB для системы + место для загрузок
- **Email** настроенный SMTP сервер для отправки писем
- **OpenSSL** для генерации CSRF токенов и шифрования

### Рекомендуемые настройки PHP:

```ini
upload_max_filesize = 10M
post_max_size = 10M
max_execution_time = 300
memory_limit = 256M
extension=curl
extension=gd
extension=openssl
```

### Поддержка аудиофайлов:

- **Браузеры** с поддержкой HTML5 Audio API
- **Форматы** MP3, WAV, OGG
- **Размер файлов** до 10MB на аудиофайл

### Безопасность:

- **HTTPS** настоятельно рекомендуется для продакшена
- **Права файлов** 644 для файлов, 755 для папок
- **Папка uploads** должна быть защищена от исполнения PHP
- **Логи** должны быть недоступны извне

## 🧹 Поддержание проекта

QuizCard включает автоматический скрипт очистки для поддержания чистоты проекта:

### Использование скрипта очистки:

```bash
./cleanup.sh
```

### Функции скрипта:

- 🗑️ **Удаление временных файлов** - .DS*Store, *.swp, \_.tmp
- 🔍 **Поиск дубликатов** - файлы с подозрительными названиями
- 🐛 **Проверка отладочного кода** - var_dump, print_r, console.log
- 📊 **Мониторинг размера логов** - предупреждения о больших файлах
- 📈 **Статистика проекта** - размер и количество файлов

### Автоматизация:

Для регулярной очистки настройте cron job:

```bash
# Запуск очистки каждый день в 3:00
0 3 * * * /path/to/quizcard/cleanup.sh >> /path/to/quizcard/logs/cleanup.log 2>&1
```

Подробнее см. файл `CLEANUP.md`.

## 🚨 Решение проблем

### Проблемы с базой данных:

1. Проверьте настройки в `config/database.php`
2. Убедитесь, что база данных существует
3. Проверьте права пользователя БД

### Проблемы с импортом:

1. Проверьте формат файла (CSV или .xlsx)
2. Убедитесь в корректности колонок
3. Проверьте размер файла (макс. 10 MB)

### Проблемы с изображениями:

1. Проверьте права записи на папку uploads/
2. Убедитесь, что URL изображений доступны
3. Поддерживаемые форматы: JPG, PNG, GIF, WebP

### Проблемы с аудиофайлами:

1. Проверьте права записи на папку uploads/audio/
2. Убедитесь в корректности аудиоформатов (MP3, WAV, OGG)
3. Проверьте размер файлов (макс. 10 MB)
4. Убедитесь, что браузер поддерживает HTML5 Audio

### Проблемы с email:

1. Проверьте настройки SMTP в файле `.env`
2. Убедитесь, что функция `mail()` работает на сервере
3. Проверьте логи сервера на ошибки отправки
4. Запустите `database/cleanup_email_tokens.php` для очистки старых токенов

### Проблемы с безопасностью:

1. **CSRF ошибки**: Очистите кэш браузера и куки
2. **Rate limiting**: Подождите некоторое время перед повторными попытками
3. **Проблемы с .env**: Проверьте права доступа к файлу (должен быть 600)
4. **Ошибки валидации**: Проверьте корректность вводимых данных
5. **Логи безопасности**: Просмотрите `logs/security.log` для диагностики

### Диагностика:

- Используйте панель безопасности в интерфейсе преподавателя
- Проверьте логи в папке `logs/`
- Запустите `./cleanup.sh` для проверки целостности проекта

## 🆕 Что нового в версии 3.0

### 🛡️ Критические улучшения безопасности:

- 🔐 **Переменные окружения** - конфиденциальные данные теперь в .env файле
- 🛡️ **CSRF Protection** - защита от межсайтовых атак
- 📝 **Валидация и санитизация** - комплексная проверка всех данных
- 🚫 **Rate Limiting** - защита от брут-форса и DDoS
- 📋 **Логирование безопасности** - детальный мониторинг событий
- 🔒 **Улучшенное хеширование** - BCRYPT с автоматическим перехешированием
- 🌐 **Защищенные .htaccess** - правила безопасности веб-сервера

### 📊 Новые возможности для преподавателей:

- 🛡️ **Панель безопасности** - мониторинг событий и статистики
- 📈 **Персонализированные логи** - только события преподавателя и его студентов
- ⚠️ **Предупреждения о безопасности** - уведомления о подозрительной активности
- 🎯 **Фильтрация событий** - анализ по времени и типу событий

### 🧹 Поддержка проекта:

- 📁 **Автоматическая очистка** - скрипт для удаления временных файлов
- 🔍 **Поиск дубликатов** - автоматическое обнаружение повторяющихся файлов
- 🐛 **Проверка отладочного кода** - поиск забытых var_dump и print_r
- 📊 **Мониторинг проекта** - статистика размера и количества файлов

### Основные нововведения версии 2.0:

- 🎵 **Поддержка аудиофайлов** - добавление и воспроизведение аудио к словам
- 📧 **Система подтверждения email** - безопасная верификация пользователей
- 🔄 **Улучшенная миграция БД** - автоматическое обновление существующих установок
- 📁 **Оптимизированная структура** - объединенный setup.sql с поддержкой всех функций

### Для обновления до версии 3.0:

1. **Создайте резервную копию** базы данных и файлов
2. **Загрузите новые файлы** проекта
3. **Скопируйте .env.example в .env** и настройте переменные
4. **Запустите database/migrate.php** для обновления БД
5. **Проверьте работу** через панель безопасности
6. **Запустите ./cleanup.sh** для оптимизации проекта

## 📁 Структура проекта

```
QuizCard/
├── app/                    # MVC ядро приложения
│   ├── Config/            # Конфигурационные файлы
│   ├── Controllers/       # Бизнес-логика (37 контроллеров)
│   ├── Models/           # Модели данных (15 классов)
│   └── Views/            # Представления и шаблоны
├── public/               # Публичная папка (входная точка)
│   ├── index.php         # Front Controller
│   ├── css/              # Стили
│   ├── js/               # JavaScript
│   └── uploads/          # Пользовательские файлы
├── database/             # Миграции и схема БД
├── docs/                 # Документация
├── vendor/               # Composer зависимости
└── old/                  # Архив предыдущих версий
```

---

**Разработано с ❤️ для безопасного и эффективного изучения языков**
